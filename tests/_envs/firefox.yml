# `firefox` environment config goes here
modules:
    enabled:
        - WebDriver
        - \Helper\Acceptance
    config:
        WebDriver:
            browser: firefox
            window_size: maximize