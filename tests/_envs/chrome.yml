# `chrome` environment config goes here
modules:
    enabled:
        - WebDriver
        - \Helper\Acceptance
    config:
        WebDriver:
            browser: chrome
            window_size: maximize